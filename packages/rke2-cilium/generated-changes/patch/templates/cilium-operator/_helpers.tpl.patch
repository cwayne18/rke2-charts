--- charts-original/templates/cilium-operator/_helpers.tpl
+++ charts/templates/cilium-operator/_helpers.tpl
@@ -32,6 +32,10 @@
 {{- $cloud := include "cilium.operator.cloud" . }}
 {{- $imageDigest := include "cilium.operator.imageDigestName" . }}
 {{- $tag := .Values.operator.image.tag | default "" | eq "" | ternary "" (printf ":%s" .Values.operator.image.tag) }}
-{{- printf "%s-%s%s%s%s" .Values.operator.image.repository $cloud .Values.operator.image.suffix $tag $imageDigest -}}
+{{- $repository := .Values.operator.image.repository -}}
+{{- if and .Values.global.prime .Values.global.prime.enabled -}}
+{{- $repository = .Values.operator.image.repository | replace "mirrored-cilium-" "hardened-cilium-" -}}
+{{- end -}}
+{{- printf "%s-%s%s%s%s" $repository $cloud .Values.operator.image.suffix $tag $imageDigest -}}
 {{- end -}}
 {{- end -}}
